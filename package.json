{
  "name": "nitpix",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Actions based visual diff system (Percy replacement)",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/"
  },
  "devDependencies": {
    "odiff-bin": "4.3.8",
    "sharp": "^0.35.0"
  }
}
